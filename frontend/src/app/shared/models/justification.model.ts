export interface Justification {
    id: string,
    nomCompletEtudiant: string,
    classeEtudiant: string,
    absenceId: string,
    commentaire: string,
    fichierUrl: string,
    statut: string,
    dateSoumission: string
}